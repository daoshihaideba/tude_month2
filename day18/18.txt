前情回顾

1. git学习
   版本   分支  远程

2. 在线词典

===========================================================
在线词典

需求分析  C/S
   客户端使用流程 见详细设计文档

功能结构划分 代码整体结构设计   (类)
   注册  登录
   查单词  历史记录

   模块和类的划分 见详细设计文档

技术点分析
   网络 : tcp网络
   并发模型 : 多进程并发
   数据库 : MySQL 数据库
   客户端两个界面如何跳转 : demo.py

数据库设计  dict 数据库  见详细设计文档
   words : id   word  mean

   user : id  name  password

   hist : id  user_id  words_id  time  降低冗余
   hist : id  name  word  time   冗余
   hist : id  word  time user_id


通信协议设计  见详细设计文档
   请求 : 请求类型    数据参数
   响应 : 每种请求的情况

网络框架搭建
   tcp 多进程网络并发   类
   服务端
        dict_server : 多进程tcp网络并发
        dict_db : 数据库处理类
   客户端
        dict_cilent : 仿照文件服务器客户端

分功能具体分析编写
   注册
       客户端:
           输入用户名 密码
           发送请求
           接收响应
           根据响应情况判断处理

       服务端
           接收请求解析
           验证是否可以注册
           发送响应分情况处理
               可以注册 : 插入数据

   登录

   查单词
      客户端 : 循环输入单词发送请求
              接收响应
                 如果查询到单词则 打印结果

      服务端 : 接收请求,查询单词 发送结果
              如果单词可以查到则 插入历史记录

   历史记录

优化完善

cookie :
密码隐藏输入
import getpass
pwd = getpass.getpass()

对在线词典客户端进行优化验证：
1. 用户名或者密码只能包含 3-12位 数字字母下划线，不能有其他
的内容且位数必须在范围内
2. 输入的查询单词 必须为大写字母小写字母下划线和-构成不能有其他字符







